# 拾念版本迭代记录

## 1.0.0：本地记忆工作台

建立“引句 + 正文材料”的记忆卡片模型，支持自由标签、日期和关键词检索、随机抽查、复习记录，以及正文模糊化和指针透视镜。

## 1.1.0：图片 Hook

为每张卡片增加独立图片编辑框。图片可以单独作为记忆提示，提示图与正文图片分开保存，并在卡片、阅读器、抽查和历史页面保持清晰显示。

## 1.2.0：存储结构优化

从单一 `memories.json` 升级为 `index.json + assets/`。图片保存为独立资源，修改单条记忆时不再重写整个图片库；同时保留旧版数据迁移和完整图片导出。

## 1.3.0：卡片检索与列表性能

加入搜索字段预计算、搜索防抖、排序缓存和分页渲染，减少大量卡片同时筛选和创建 DOM 的开销。

## 1.4.0：启动体验优化

窗口先显示，再在后台读取索引和迁移数据，减少启动时的空白等待；继续继承 1.3 的检索和列表优化。
